## A GitHub.io-clone of hedman.de packed in a handy Docker setup

My own homepage as a GitHub.io clone: a pur HTML5-, JavaScript- and CSS-Site. <br /> 
Here it's packed in a Docker container, to get it runing by a only few steps.

My offical johanhedman.com and hedman.de Homepage though, is built with Laravel in the backend and runs with a React.js frontend.